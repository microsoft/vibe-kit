# BioEMU scoring container dependencies
# -------------------------------------------------
# BioEMU 1.3.1 brings its own torch>=2.6.0, torch_geometric, hydra,
# numpy, mdtraj, fire, huggingface_hub, etc. — don't duplicate them.
bioemu[cuda]==1.3.1

# Serving
flask>=3.0.0
gunicorn>=22.0.0
