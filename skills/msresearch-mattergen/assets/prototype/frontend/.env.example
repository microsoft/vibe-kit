# MatterGen/MatterSim Prototype Environment Variables

# External Deployment Mode
# Set to 'true' to show the "Back to AI for Science Labs" link in the header
# This should be enabled when deploying externally
VITE_EXTERNAL=false

# API Base URL (optional - defaults to relative /api path)
# VITE_API_BASE_URL=http://localhost:8000
