MATTERGEN_ENDPOINT_URL=
MATTERGEN_USE_ENTRA_AUTH=true

MATTERSIM_ENDPOINT_URL=
MATTERSIM_USE_ENTRA_AUTH=true

# Application mode: "research" or "production" (affects available model features)
APP_MODE=production

# Demo mode: When true, enables fallback to pre-generated demo data if the real API fails
# This is useful for demos/testing when the MatterGen endpoint is unavailable
DEMO_MODE=false
