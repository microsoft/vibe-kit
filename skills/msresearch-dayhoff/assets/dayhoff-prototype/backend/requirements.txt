# Dayhoff Prototype Requirements
# Install these dependencies to run the Dayhoff protein sequence generator

# Core ML libraries
torch>=2.0.0
transformers>=4.30.0

# Web framework
flask>=2.3.0
flask-cors>=4.0.0
python-dotenv>=1.0.0

# HTTP requests
requests>=2.31.0

# Optional: Jupyter notebook support
jupyter>=1.0.0
ipywidgets>=8.0.0

# Optional: Better progress bars
tqdm>=4.65.0

# Optional: Scientific computing (if adding analysis features)
numpy>=1.24.0
pandas>=2.0.0

# Note: The transformers library will automatically download the Dayhoff model
# on first use. Make sure you have:
# - Internet connection for model download
# - At least 2GB free disk space
# - Python 3.8 or higher