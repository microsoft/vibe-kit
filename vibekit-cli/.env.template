# Development environment variables for vibekit CLI
# Only non-secret variables are listed here. Copy to .env and adjust as needed.
# Lines formatted as KEY=VALUE (no quotes). Blank lines and comments are ignored.

# Path to the local innovation kit repository root (folder containing kit manifests)
# Can be absolute or relative to where you invoke vibekit commands.
VIBEKIT_BASE_PATH=../innovation-kit-repository

# Optional override for template repo used during `vibekit init`.
# Leave unset to use the default remote.
VIBEKIT_INIT_REPO_URL=https://github.com/microsoft/vibe-kit

# Personal Access Token (PAT) - DO NOT COMMIT REAL VALUE
# ---------------------------------------------------------------------------
# Provide GIT_PAT when cloning private template repos during `vibekit init`.
# Keep only a blank or placeholder here in .env.dev and export the real value in your shell.

# GIT PAT (required for private repo access)
GIT_PAT=

# IMPORTANT: Leave this blank or with a dummy placeholder in version-controlled files.
